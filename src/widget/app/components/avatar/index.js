import tw from 'twin.macro';

const AvatarStyles = tw.div`flex`;

const Avatar = () => {

    return (
        <AvatarStyles>
            <p>AVATAR</p>
        </AvatarStyles>
    );
};

export default Avatar;