import DynamicSvg from '../dynamic-svg';

const MobalyticsLogo = ({ className }) => {

    return (
        <DynamicSvg name="mobalytics-logo" className={className} />
    );
}

export default MobalyticsLogo;